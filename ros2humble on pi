# Ensure UTF-8 locale
sudo apt update && sudo apt install locales -y
sudo locale-gen en_US en_US.UTF-8
sudo update-locale LC_ALL=en_US.UTF-8 LANG=en_US.UTF-8
export LANG=en_US.UTF-8

# Add the ROS 2 GPG key
sudo apt install software-properties-common -y
sudo add-apt-repository universe -y
sudo curl -sSL https://raw.githubusercontent.com/ros/rosdistro/master/ros.key -o /usr/share/keyrings/ros-archive-keyring.gpg

# Add the repository to your sources list
echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/ros-archive-keyring.gpg] http://packages.ros.org/ros2/ubuntu $(. /etc/os-release && echo $UBUNTU_CODENAME) main" | sudo tee /etc/list.d/ros2.list > /dev/null

once installed - after purple screen
nano ~/.bashrc
-here go to the end and copy paste this- # Source ROS 2 Humble Base
                      source /opt/ros/humble/setup.bash

# Set a matching network Domain ID (pick any number between 0 and 232)
                       export ROS_DOMAIN_ID=42

# Force discovery across your hotspot subnet
                        export ROS_AUTOMATIC_DISCOVERY_RANGE=SUBNET

exit and enter source ~/.bashrc, done
